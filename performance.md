# 性能测试数据

## 环境

- Host CPU: i9-14900HX（24 逻辑核）
- 虚拟机: 8 vCPU
- 工作负载: `[0, 2^32)` 候选值 × AVX2 8-wide 整数比较

---

## 基线 vs 实验组

| 版本 | 耗时 | 加速比 |
|------|------|--------|
| 基线 `SearchRandom`（单线程） | ~13 s | 1× |
| 实验组（24 线程，最优） | **0.109 s** | **~119×** |

---

## 多线程数扫描（8 vCPU 虚拟机）

| 线程数 | real (s) | user (s) | sys (s) | user/real ≈ 实际并行度 | 相对 4 线程加速 |
|--------|----------|----------|---------|------------------------|------------------|
| 4      | 0.237    | 0.926    | 0.002   | 3.9                    | 1.00×            |
| 8      | 0.165    | 1.201    | 0.002   | 7.3                    | 1.43×            |
| 16     | 0.129    | 1.544    | 0.006   | 12.0                   | 1.84×            |
| **24** | **0.109**| 2.001    | 0.009   | **18.4**               | **2.17× ← 最优** |
| 32     | 0.114    | 2.045    | 0.013   | 17.9                   | 2.08×            |
| 64     | 0.110    | 2.253    | 0.017   | 20.5                   | 2.15×            |

---

## 关键观察

### 1. 超线性加速：8 vCPU 虚拟机里跑出 18× 并行度

24 线程版 `user/real` 达到 **18.4**，远超 guest 看到的 8 vCPU。原因：VMware / VirtualBox 这类虚拟化层默认不把 vCPU 硬绑在固定数量的物理核上，每个 vCPU 是 host 上的一个线程，host 调度器可以在 i9-14900HX 的 24 逻辑核上自由迁移它们。"分配 8 核"只限制 guest 内同时刻的并发数，**不限制真实 CPU 时间**。

### 2. 最优线程数 ≈ 物理核数 × 2–3

- 8 线程只榨了约 **40%** 真实算力
- 24 线程达到峰值（约 3× vCPU 数）
- 32 → 64 几乎不再加速，物理资源饱和

**机制**：AVX2 整数流水线有延迟，单线程吃不满 ALU。每核跑 2–3 个线程能掩盖流水线停顿与 L3 miss，SMT 效应在密码穷举这种 ILP 低的负载上尤其明显。

### 3. `user + sys ≠ real`

对多线程程序，`time` 命令的三个数字含义不同：

| 字段 | 含义 |
|------|------|
| `real` | 墙钟时间（从启动到结束） |
| `user` | 所有线程在用户态的 CPU 时间**累加** |
| `sys`  | 所有线程在内核态的 CPU 时间**累加** |

估算 CPU 利用率：`(user + sys) / real`，比值越接近真实运行的物理核数，说明越饱和。

---

## 结论

- **推荐配置：24 线程**（与 `run.sh sweep` 默认档位一致）
- 在 8 vCPU 虚拟机上跑这个工作负载，超过 32 线程收益微小
- 这组数据是「虚拟化层不硬限制 vCPU」+「SMT 掩盖 SIMD 流水线延迟」两个效应叠加的典型例子
