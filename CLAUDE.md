# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 在此仓库中工作时提供指导。

## 项目概述

CS 课程实验四（NUDT）。包含两个 Python 实现的 **Toy Machine** CPU 模拟器——一个使用数值编码指令，另一个使用助记符（文本）编码指令。

## 文件说明

- `toy.py` — 数值编码 Toy Machine 模拟器。指令为 5 位整数：`OORAA`，其中 `OO`=操作码，`R`=寄存器(0–9)，`AA`=地址(0–999)。
- `toy2.py` — 助记符编码 Toy Machine 模拟器。指令为文本格式，如 `mov3 1 12`、`add 1 2`。
- `test.toy` — 数值格式示例程序（计算 31 + 32）。
- `test.toy2` — 助记符格式示例程序（计算 12 + 13）。

## 运行方式

```bash
python3 toy.py    # 运行 test.toy（路径已硬编码，本地使用可能需要修改）
python3 toy2.py   # 运行 test.toy2（同上）
```

两个脚本的输入文件路径均硬编码在文件底部 `run()` 的参数中，更换程序时需手动修改。

## 架构

两个模拟器共享相同的"取指-译码-执行"循环和指令集：

- **状态**：1000 单元内存数组、10 个寄存器、程序计数器(`pReg`)、指令寄存器(`iReg`)
- **指令集**：`mov1`（内存→寄存器）、`mov2`（寄存器→内存）、`mov3`（立即数→寄存器）、`mov4`（寄存器间接寻址）、`add`、`sub`、`mul`、`div`、`jmp`、`jz`、`in`、`out`、`halt`
- `toy.py` 使用数值操作码（0=halt, 1=mov1, ..., 10=jmp, 11=jz）；`toy2.py` 使用字符串助记符

### 数值指令格式（`toy.py`）

| 数位 | 含义 |
|------|------|
| `OO`（÷10000） | 操作码 |
| `R`（÷1000 % 10） | 寄存器编号 |
| `AAA`（% 1000） | 地址/立即数 |

### 程序文件格式

每行格式：`<地址> <指令>`（数值）或 `<地址> <助记符> [操作数1] [操作数2]`（助记符）。
