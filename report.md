# 计算机系统实验 12 汇报

**任务**：把单线程密码穷举 `SearchRandom.c`（基线 ~13 s）改写为 **多线程 + AVX2 + 内嵌汇编** 版 `Thread_202402720028.c`，在 `[0, 2^32)` 范围内找出全部 3 个密码。

---

## 一、三层加速

| 层级 | 技术 | 关键点 |
|------|------|--------|
| 1 | **pthread 多线程** | 区间均匀划分；用 uint32 自然回绕处理最后一个线程的端点（避免特判）；互斥锁保护 `printf` |
| 2 | **AVX2 SIMD** | 8-wide 整数比较，循环步长 1 → 8，单条指令处理 8 个候选值 |
| 3 | **内嵌汇编** | `asm volatile` 直接操作 YMM 寄存器，绕过编译器中间 store / load；命中后跳出，不在 asm 里 `call printf` |

---

## 二、性能（i9-14900HX host, 8 vCPU VM）

### 基线 vs 实验组

| 版本 | 耗时 | 加速比 |
|------|------|--------|
| 基线 `SearchRandom`（单线程） | ~13 s | 1× |
| **实验组（24 线程）** | **0.109 s** | **~119×** |

### 线程数扫描

| 线程数 | 4 | 8 | 16 | **24** | 32 | 64 |
|--------|---|---|----|----|----|----|
| real (s) | 0.237 | 0.165 | 0.129 | **0.109** | 0.114 | 0.110 |

---

## 三、关键发现：超线性加速

24 线程版的 `user / real = 18.4`，**远超 guest 看到的 8 vCPU**。原因有二：

1. **虚拟化层不硬限制 vCPU** —— 每个 vCPU 是 host 上的一个线程，host 调度器可以在 24 个逻辑核上自由迁移它们。"分配 8 核"只限制 guest 内同时刻并发数，**不限制真实 CPU 时间**。
2. **SMT 效应** —— AVX2 整数流水线有延迟，单线程吃不满 ALU；每核跑 2–3 个线程能掩盖流水线停顿和 L3 miss。密码穷举这种 ILP 低的负载特别受益。

**结论**：最优线程数 ≈ **物理核数 × 2–3**，而不是 1:1。

---

## 四、`time` 输出含义（汇报时可能被问）

| 字段 | 含义 |
|------|------|
| `real` | 墙钟时间（从启动到结束） |
| `user` | 所有线程**用户态** CPU 时间累加 |
| `sys`  | 所有线程**内核态** CPU 时间累加 |

多线程程序里 `user + sys` **可以大于 `real`**（多核累加）。CPU 利用率 ≈ `(user + sys) / real`。

---

## 五、总结

- 三层加速把 13 s 压到 **0.109 s**，加速比 **~119×**
- 最优线程数（24）远超 vCPU 数（8），是虚拟化层不硬限制 + SMT 效应共同作用的结果
- 详细数据见 [`performance.md`](./performance.md)，设计思路见 [`helpme.md`](./helpme.md)
